[English](./CONCEPT.md) | [日本語](./CONCEPT.ja.md)

# morphink — Vue 3 のためのデザインシステムボイラープレート

## morphink とは

morphink は UI コンポーネントライブラリではありません。**自分たちのデザインシステムを構築するためのボイラープレート**です。

チームが Vue 3 でデザインシステムを作ろうとすると、必ず一連の構造的な判断に直面します。トークンパイプラインをどう設計するか、モノレポをどう構成するか、コンポーネントのアーキテクチャをどうするか、Storybook をどうセットアップするか、デザインツールとどう接続するか。morphink はこれらすべてに対する**検証済みの回答**を一度に提供します。

このリポジトリをフォークし、トークンを自分のブランドに差し替えれば、プロダクトとともに成長できるデザインシステムの準備が整います。

## なぜ morphink が必要なのか

shadcn/ui のようなツールは、プロジェクトにコピーできる優れたコンポーネントプリミティブを提供します。しかし、デザインシステムの構築にはコンポーネント以上のもの——インフラが必要です。

- デザインツールとコードを接続するトークンパイプライン
- 内部実装を隔離する層構造のコンポーネントアーキテクチャ
- トークン駆動のテーマ切り替え（ダークモード含む）
- 適切なビルドオーケストレーションを持つモノレポ構成
- Storybook によるドキュメント

shadcn + Tailwind + 手動セットアップでこれらすべてを自力で組み上げることは*できます*。morphink はその手間を省き、**検証済みの出発点**を提供します——理論ではなく、実際の試行錯誤の結果として。

## アーキテクチャ: アトミックデザインではなく、垂直レイヤー

morphink のコンポーネントアーキテクチャは 3 つの垂直レイヤーで構成されています。

```
Public (components/)  →  Base (base/ui/*/)  →  Reka UI
  プロダクト API          スタイルバリアント      ヘッドレス a11y
```

これはアトミックデザインとは根本的に異なるものです。アトミックデザインは合成の粒度によってコンポーネントを**水平方向に**分類します（Atom → Molecule → Organism）。morphink の 3 層は、単一コンポーネントの内部を**垂直方向に**抽象度で分類しています。

| | アトミックデザイン | morphink 3 層構造 |
|---|---|---|
| 軸 | 水平（合成） | 垂直（抽象度） |
| 答える問い | 「コンポーネントをどう組み合わせるか？」 | 「コンポーネントの内部をどう作るか？」 |
| スコープ | コンポーネント間の関係 | コンポーネント内の責務分離 |

この 2 つの概念は競合しません。morphink のコンポーネントを使い、プロダクト側でアトミックデザインの原則に従って組み立てることは普通にできます。morphink は Atom や Molecule の**作り方**を扱い、それをどう**組み合わせる**かは使用者の判断に委ねています。

### なぜ 3 層なのか

最大の利点は**依存ライブラリからの絶縁**です。

もし Reka UI の開発が停止したり、より良いヘッドレスライブラリが登場した場合——変更が必要なのは Base 層だけです。Public の API（`<Button tone="primary">`）は安定したまま。Tailwind に破壊的変更が来ても——吸収するのは Base 層だけ。デザインシステムの利用者は何も知らなくて構いません。

同じ原則がトークンパイプラインにも当てはまります。トークンの値は alias → semantic → CSS 変数 → コンポーネントと流れます。デザインチームが色を更新したとき、変更はコンポーネントのコードに触れることなく自動的に伝播します。

### なぜ Reka UI なのか

morphink はヘッドレス a11y レイヤーとして [Reka UI](https://reka-ui.com/)（旧 Radix Vue）を採用しています。選定の決め手は Vue 3 エコシステムとの適合性です。Reka UI は WAI-ARIA 準拠のプリミティブを最も幅広く提供し（Dialog, Select, Dropdown, Tabs, Popover, Tooltip 等）、Vue 3 のファーストクラスサポートと `useForwardPropsEmits` によるクリーンな controlled/uncontrolled 状態管理を備えています。Headless UI はプリミティブの種類が少なく、Ark UI は有望ながら選定時点ではコミュニティの採用が限定的でした。Base 層がこの依存を隔離しているため、選択は永続的ではありません——別のヘッドレスライブラリへの切り替えは Base 層にのみ影響し、プロダクトコードには波及しません。

### Public 層: 意図的な拡張ポイント

現在、Public 層は Base の薄いラッパーです。これは意図的なものです。

Public 層は将来、**プロダクトレベルのデザイン判断を焼き込む場所**になるよう設計されています。たとえば、ラベル・エラーメッセージ・ヘルパーテキストを標準レイアウトでバンドルした Input コンポーネントなど。しかし、そうした判断は実際のプロダクト利用から生まれるべきであり、推測からではありません。

それまでの間、この層は薄いまま保ち、アーキテクチャは拡張の準備ができた状態を維持します。

## トークンパイプライン

morphink のトークンシステムは 2 層構造に従い、生の値にデザインの意図を付与してからコンポーネントに届けます。

```
alias.json                  semantic.json               CSS 変数                       コンポーネント
──────────────────────────  ──────────────────────────  ────────────────────────────  ──────────────────────────
color.primary-500:          color.primary.base:         --morphink-color-primary:     bg-(--morphink-color-primary)
  oklch(65.96% 0.21 18)      refs {color.primary-500}    resolved to oklch(...)
```

**Alias トークン**は生のパレットを定義します——カラースケール、スペーシング値、角丸。これらはデザイナーや開発者が直接参照することの少ない実装の詳細です。

**Semantic トークン**は意味を割り当てます——「primary」「destructive」「muted」。これがデザインシステムの語彙です。コンポーネントは semantic トークンのみを参照し、alias の値を直接使うことはありません。

この分離により、alias.json を差し替えるだけで、コンポーネントファイルに一切触れることなくビジュアルアイデンティティを完全に変更できます。

## morphink でないもの

- **npm install するパッケージではありません。** リポジトリをフォークし、コードを自分で所有します。
- **shadcn/ui の代替ではありません。** morphink は shadcn のコンポーネントパターン（ヘッドレスプリミティブ + CVA スタイリング）に影響を受けていますが、解決する問題が異なります——shadcn が提供しないインフラ層、すなわちトークンパイプライン、テーマ管理、モノレポオーケストレーション、レイヤードアーキテクチャを提供します。
- **ページ構成に意見を持ちません。** よく作られたコンポーネントを提供し、それをどうページに組み立てるかは使用者次第です。

## 誰のための morphink か

- デザインシステムをゼロから構築するのではなく、検証済みのアーキテクチャから始めたい**チーム**
- 動作する文書化されたコードベースを通じてデザインシステム構築を学びたい**個人開発者**
- shadcn/ui を使って「これは素晴らしいけど、トークン管理、テーミング、適切なパッケージ構成がこの上に欲しい」と感じた**すべての人**

## はじめ方

```bash
# 1. GitHub で「Use this template」をクリック
# 2. 新しいリポジトリをクローン
# 3. インストールして起動
pnpm install
pnpm run build
pnpm run dev:docs
```

あとは自分のものにしてください：

1. `packages/tokens/tokens/` のトークン値をブランドカラーとスケールに差し替え
2. 再ビルド：`pnpm run build`
3. デザインシステムの準備完了

## ライセンス

[MIT](./LICENSE)
