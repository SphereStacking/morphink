import { Meta, Title, Subtitle, Canvas, Story } from '@storybook/blocks'
import * as ButtonStories from '../components/Button.stories'
import * as TabsStories from '../components/Tabs.stories'
import * as DialogStories from '../components/Dialog.stories'
import * as SelectStories from '../components/Select.stories'
import * as GridStories from '../components/Grid.stories'
import * as StackStories from '../components/Stack.stories'
import * as HeadingStories from '../components/Heading.stories'
import * as TextStories from '../components/Text.stories'
import * as PanelStories from '../components/Panel.stories'
import * as DividerStories from '../components/Divider.stories'
import * as SectionHeaderStories from '../components/SectionHeader.stories'

<Meta title="Docs/Components Overview" />

<Title>コンポーネント概要</Title>
<Subtitle>共通Propsと状態管理の使い方</Subtitle>

このページでは、ink-ui の共通Props（variant/size/tone/density）と、
状態管理（open/onOpenChange、modelValue/update:modelValue）の使い方を例中心で示します。
A11y は重大な問題の抑制を優先し、軽微な警告は状況に応じて判断します。

## 共通Props（Button）

<Canvas of={ButtonStories.Overview} />

## 状態管理（Tabs）

<Canvas of={TabsStories.Overview} />

## 状態管理（Dialog）

<Canvas of={DialogStories.Overview} />

## フォーム系（Select）

<Canvas of={SelectStories.Overview} />

## レイアウト（Grid / Stack）

<Canvas of={GridStories.Overview} />

<Canvas of={StackStories.Overview} />

## タイポグラフィ（Heading / Text）

<Canvas of={HeadingStories.Overview} />

<Canvas of={TextStories.Overview} />

## サーフェス（Panel / Divider）

<Canvas of={PanelStories.Overview} />

<Canvas of={DividerStories.Overview} />

## 情報整理（SectionHeader）

<Canvas of={SectionHeaderStories.Overview} />

## A11y（方針）

- 重大な警告を優先して解消します。
- Dialog / Tabs はキーボード操作（Tab / Esc / Arrow）を確認してください。
- Tooltip/Popover/Dropdown もフォーカス移動を確認します。
