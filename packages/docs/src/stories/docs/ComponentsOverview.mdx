import { Meta, Title, Subtitle, Canvas, Story } from '@storybook/addon-docs/blocks'
import * as ButtonStories from '../components/atoms/Button.stories'
import * as TabsStories from '../components/atoms/Tabs.stories'
import * as DialogStories from '../components/atoms/Dialog.stories'
import * as SelectStories from '../components/atoms/Select.stories'
import * as GridStories from '../components/atoms/Grid.stories'
import * as StackStories from '../components/atoms/Stack.stories'
import * as HeadingStories from '../components/atoms/Heading.stories'
import * as TextStories from '../components/atoms/Text.stories'
import * as PanelStories from '../components/atoms/Panel.stories'
import * as DividerStories from '../components/atoms/Divider.stories'
import * as SectionHeaderStories from '../components/atoms/SectionHeader.stories'

<Meta title="Docs/Components Overview" />

<Title>Components Overview</Title>
<Subtitle>How to use common props and state management</Subtitle>

This page demonstrates the usage of morphink's common props (variant/size/tone/density) and
state management patterns (v-model:open, v-model / update:modelValue) through examples.
A11y prioritizes resolving critical issues; minor warnings are addressed as needed.

## Common Props (Button)

<Canvas of={ButtonStories.Variants} />

<Canvas of={ButtonStories.Tones} />

<Canvas of={ButtonStories.Sizes} />

## State Management (Tabs)

<Canvas of={TabsStories.Default} />

## State Management (Dialog)

<Canvas of={DialogStories.Default} />

## Form Components (Select)

<Canvas of={SelectStories.Default} />

## Layout (Grid / Stack)

<Canvas of={GridStories.Default} />

<Canvas of={StackStories.Default} />

## Typography (Heading / Text)

<Canvas of={HeadingStories.Default} />

<Canvas of={TextStories.Default} />

## Surfaces (Panel / Divider)

<Canvas of={PanelStories.Default} />

<Canvas of={DividerStories.Default} />

## Content Organization (SectionHeader)

<Canvas of={SectionHeaderStories.Default} />

## A11y (Policy)

- Prioritize resolving critical warnings.
- Verify keyboard navigation (Tab / Esc / Arrow) for Dialog and Tabs.
- Verify focus management for Tooltip, Popover, and Dropdown.
