import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks'

export const TokenGrid = ({ children }) => (
  <div
    style={{
      display: 'grid',
      gap: '12px',
      gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
    }}
  >
    {children}
  </div>
)

export const TokenCard = ({ token, cssVar, usage, preview }) => (
  <div
    style={{
      border: '1px solid var(--morphink-color-border)',
      borderRadius: 'var(--morphink-radius-md)',
      padding: '12px',
      background: 'var(--morphink-color-card)',
    }}
  >
    <div title={cssVar}>{preview}</div>
    <div
      style={{
        marginTop: '8px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Token
    </div>
    <div>
      <code>{token}</code>
    </div>
    <div
      style={{
        marginTop: '6px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      CSS Var
    </div>
    <div>
      <code>{cssVar}</code>
    </div>
    {usage ? (
      <div
        style={{
          marginTop: '6px',
          fontSize: 'var(--morphink-font-size-caption)',
          lineHeight: 'var(--morphink-font-line-height-caption)',
          color: 'var(--morphink-color-muted-foreground)',
        }}
      >
        {usage}
      </div>
    ) : null}
  </div>
)

export const ScaleRow = ({ title, tokens }) => (
  <div style={{ display: 'grid', gap: '10px' }}>
    <div
      style={{
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
        letterSpacing: '0.02em',
        textTransform: 'uppercase',
      }}
    >
      {title}
    </div>
    <div
      style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(9, minmax(0, 1fr))',
        gap: '8px',
      }}
    >
      {tokens.map(({ label, cssVar }) => (
        <div key={cssVar} style={{ display: 'grid', gap: '6px' }}>
          <div
            className="color-scale-swatch"
            data-tooltip={cssVar}
            style={{
              height: '40px',
              borderRadius: 'var(--morphink-radius-sm)',
              background: `var(${cssVar})`,
              border: '1px solid var(--morphink-color-border)',
            }}
          />
          <div
            style={{
              fontSize: 'var(--morphink-font-size-caption)',
              lineHeight: 'var(--morphink-font-line-height-caption)',
              color: 'var(--morphink-color-muted-foreground)',
            }}
          >
            <div>{label}</div>
          </div>
        </div>
      ))}
    </div>
  </div>
)

export const scale = (name) => [
  { label: '100', cssVar: `--morphink-color-${name}-100` },
  { label: '200', cssVar: `--morphink-color-${name}-200` },
  { label: '300', cssVar: `--morphink-color-${name}-300` },
  { label: '400', cssVar: `--morphink-color-${name}-400` },
  { label: '500', cssVar: `--morphink-color-${name}-500` },
  { label: '600', cssVar: `--morphink-color-${name}-600` },
  { label: '700', cssVar: `--morphink-color-${name}-700` },
  { label: '800', cssVar: `--morphink-color-${name}-800` },
  { label: '900', cssVar: `--morphink-color-${name}-900` },
]

<style>{`
.color-scale-swatch {
  position: relative;
}

.color-scale-swatch::after {
  content: attr(data-tooltip);
  position: absolute;
  left: 50%;
  bottom: 100%;
  transform: translate(-50%, -6px);
  padding: 4px 6px;
  border-radius: 4px;
  font-size: 11px;
  line-height: 1.2;
  background: var(--morphink-color-foreground);
  color: var(--morphink-color-background);
  border: 1px solid var(--morphink-color-border);
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.12s ease;
  z-index: 10;
}

.color-scale-swatch:hover::after {
  opacity: 1;
}
`}</style>

<Meta title="Foundations/Color" />

<Title>Color</Title>
<Subtitle>Foundations for role-based color usage</Subtitle>

## Overview

Color communicates meaning and state. morphink defines tokens by semantic role, avoiding direct color specifications.

## Token List

### Surface

#### Base

<TokenGrid>
  <TokenCard
    token="color.background.base"
    cssVar="--morphink-color-background"
    usage="Background"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-background)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.foreground.base"
    cssVar="--morphink-color-foreground"
    usage="Primary text"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

#### Muted

<TokenGrid>
  <TokenCard
    token="color.muted.base"
    cssVar="--morphink-color-muted"
    usage="Muted surface"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-muted)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.muted.foreground"
    cssVar="--morphink-color-muted-foreground"
    usage="Secondary text"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-muted-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

#### Card

<TokenGrid>
  <TokenCard
    token="color.card.base"
    cssVar="--morphink-color-card"
    usage="Card surface"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.card.foreground"
    cssVar="--morphink-color-card-foreground"
    usage="Card text"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

#### Popover

<TokenGrid>
  <TokenCard
    token="color.popover.base"
    cssVar="--morphink-color-popover"
    usage="Popover surface"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-popover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.popover.foreground"
    cssVar="--morphink-color-popover-foreground"
    usage="Popover text"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-popover-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

#### Utility

<TokenGrid>
  <TokenCard
    token="color.border.base"
    cssVar="--morphink-color-border"
    usage="Border"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-border)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.input.base"
    cssVar="--morphink-color-input"
    usage="Input background"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-input)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Primary

<TokenGrid>
  <TokenCard
    token="color.primary.base"
    cssVar="--morphink-color-primary"
    usage="→ color.primary-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-primary)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.primary.foreground"
    cssVar="--morphink-color-primary-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-primary-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.primary.hover"
    cssVar="--morphink-color-primary-hover"
    usage="→ color.primary-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-primary-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.primary.active"
    cssVar="--morphink-color-primary-active"
    usage="→ color.primary-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-primary-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Secondary

<TokenGrid>
  <TokenCard
    token="color.secondary.base"
    cssVar="--morphink-color-secondary"
    usage="→ color.secondary-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-secondary)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.secondary.foreground"
    cssVar="--morphink-color-secondary-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-secondary-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.secondary.hover"
    cssVar="--morphink-color-secondary-hover"
    usage="→ color.secondary-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-secondary-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.secondary.active"
    cssVar="--morphink-color-secondary-active"
    usage="→ color.secondary-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-secondary-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Tertiary

<TokenGrid>
  <TokenCard
    token="color.tertiary.base"
    cssVar="--morphink-color-tertiary"
    usage="→ color.tertiary-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-tertiary)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.tertiary.foreground"
    cssVar="--morphink-color-tertiary-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-tertiary-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.tertiary.hover"
    cssVar="--morphink-color-tertiary-hover"
    usage="→ color.tertiary-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-tertiary-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.tertiary.active"
    cssVar="--morphink-color-tertiary-active"
    usage="→ color.tertiary-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-tertiary-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Base

<TokenGrid>
  <TokenCard
    token="color.base.base"
    cssVar="--morphink-color-base"
    usage="→ color.base-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-base)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.base.foreground"
    cssVar="--morphink-color-base-foreground"
    usage="→ color.gray-900"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-base-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.base.hover"
    cssVar="--morphink-color-base-hover"
    usage="→ color.base-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-base-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.base.active"
    cssVar="--morphink-color-base-active"
    usage="→ color.base-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-base-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Accent

<TokenGrid>
  <TokenCard
    token="color.accent.base"
    cssVar="--morphink-color-accent"
    usage="→ color.accent-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-accent)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.accent.foreground"
    cssVar="--morphink-color-accent-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-accent-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.accent.hover"
    cssVar="--morphink-color-accent-hover"
    usage="→ color.accent-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-accent-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.accent.active"
    cssVar="--morphink-color-accent-active"
    usage="→ color.accent-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-accent-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Neutral

<TokenGrid>
  <TokenCard
    token="color.neutral.base"
    cssVar="--morphink-color-neutral"
    usage="→ color.gray-900"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-neutral)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.neutral.foreground"
    cssVar="--morphink-color-neutral-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-neutral-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.neutral.hover"
    cssVar="--morphink-color-neutral-hover"
    usage="→ color.black"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-neutral-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.neutral.active"
    cssVar="--morphink-color-neutral-active"
    usage="→ color.black"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-neutral-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Success

<TokenGrid>
  <TokenCard
    token="color.success.base"
    cssVar="--morphink-color-success"
    usage="→ color.success-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-success)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.success.foreground"
    cssVar="--morphink-color-success-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-success-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.success.hover"
    cssVar="--morphink-color-success-hover"
    usage="→ color.success-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-success-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.success.active"
    cssVar="--morphink-color-success-active"
    usage="→ color.success-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-success-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Warning

<TokenGrid>
  <TokenCard
    token="color.warning.base"
    cssVar="--morphink-color-warning"
    usage="→ color.warning-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-warning)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.warning.foreground"
    cssVar="--morphink-color-warning-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-warning-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.warning.hover"
    cssVar="--morphink-color-warning-hover"
    usage="→ color.warning-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-warning-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.warning.active"
    cssVar="--morphink-color-warning-active"
    usage="→ color.warning-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-warning-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Info

<TokenGrid>
  <TokenCard
    token="color.info.base"
    cssVar="--morphink-color-info"
    usage="→ color.info-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-info)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.info.foreground"
    cssVar="--morphink-color-info-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-info-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.info.hover"
    cssVar="--morphink-color-info-hover"
    usage="→ color.info-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-info-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.info.active"
    cssVar="--morphink-color-info-active"
    usage="→ color.info-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-info-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

### Destructive

<TokenGrid>
  <TokenCard
    token="color.destructive.base"
    cssVar="--morphink-color-destructive"
    usage="→ color.destructive-500"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-destructive)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.destructive.foreground"
    cssVar="--morphink-color-destructive-foreground"
    usage="→ color.white"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-destructive-foreground)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.destructive.hover"
    cssVar="--morphink-color-destructive-hover"
    usage="→ color.destructive-600"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-destructive-hover)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="color.destructive.active"
    cssVar="--morphink-color-destructive-active"
    usage="→ color.destructive-700"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-destructive-active)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

## Color Scales

Color shades progress from light (100) to dark (900). Each scale is used based on UI state.

<div style={{ display: 'grid', gap: '24px' }}>
  <ScaleRow title="Primary" tokens={scale('primary')} />
  <ScaleRow title="Secondary" tokens={scale('secondary')} />
  <ScaleRow title="Tertiary" tokens={scale('tertiary')} />
  <ScaleRow title="Base" tokens={scale('base')} />
  <ScaleRow title="Accent" tokens={scale('accent')} />
  <ScaleRow title="Success" tokens={scale('success')} />
  <ScaleRow title="Warning" tokens={scale('warning')} />
  <ScaleRow title="Info" tokens={scale('info')} />
  <ScaleRow title="Destructive" tokens={scale('destructive')} />
</div>

## Utilities

```
mi:bg-{color}
mi:text-{color}
mi:border-{color}
```

Example: `mi:bg-primary`, `mi:text-foreground`, `mi:border-destructive`

## Notes for designers

- Brand colors are organized under `color.primary.base` / `color.secondary.base` / `color.tertiary.base` / `color.accent.base`.
- Text colors use `color.foreground.base` and `color.muted.foreground`.
- Surfaces are organized under `color.background.base` / `color.card.base` / `color.muted.base`.
- Borders use `color.border.base`.
