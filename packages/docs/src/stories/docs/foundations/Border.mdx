import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks'

export const TokenGrid = ({ children }) => (
  <div
    style={{
      display: 'grid',
      gap: '12px',
      gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
    }}
  >
    {children}
  </div>
)

export const TokenCard = ({ token, value, preview }) => (
  <div
    style={{
      border: '1px solid var(--morphink-color-border)',
      borderRadius: 'var(--morphink-radius-md)',
      padding: '12px',
      background: 'var(--morphink-color-card)',
    }}
  >
    <div>{preview}</div>
    <div
      style={{
        marginTop: '8px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Token
    </div>
    <div>
      <code>{token}</code>
    </div>
    <div
      style={{
        marginTop: '6px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Value
    </div>
    <div>
      <code>{value}</code>
    </div>
  </div>
)

<Meta title="Foundations/[TBD] Border" />

<Title>Border</Title>
<Subtitle>Organize border standards</Subtitle>

## Overview

A page to organize border width and stroke specifications.

## Token List

<TokenGrid>
  <TokenCard
    token="border.width.default"
    value="1px"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card)',
          border: 'var(--morphink-border-width-default) solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="border.width.strong"
    value="2px"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card)',
          border: 'var(--morphink-border-width-strong) solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="border.width.1"
    value="1px"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card)',
          border: 'var(--border-width1) solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="border.width.2"
    value="2px"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-card)',
          border: 'var(--border-width2) solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

## Notes for designers

TBD
