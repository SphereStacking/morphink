import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks'

export const TokenGrid = ({ children }) => (
  <div
    style={{
      display: 'grid',
      gap: '12px',
      gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
    }}
  >
    {children}
  </div>
)

export const TokenCard = ({ token, value, preview }) => (
  <div
    style={{
      border: '1px solid var(--morphink-color-border)',
      borderRadius: 'var(--morphink-radius-md)',
      padding: '12px',
      background: 'var(--morphink-color-card)',
    }}
  >
    <div>{preview}</div>
    <div
      style={{
        marginTop: '8px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Token
    </div>
    <div>
      <code>{token}</code>
    </div>
    <div
      style={{
        marginTop: '6px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Value
    </div>
    <div>
      <code>{value}</code>
    </div>
  </div>
)

<Meta title="Foundations/Opacity" />

<Title>Opacity</Title>
<Subtitle>Organize opacity token standards</Subtitle>

## Overview

A page to organize opacity tokens and their usage.

## Token List

<TokenGrid>
  <TokenCard
    token="opacity.40"
    value="0.4"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-40)',
        }}
      />
    }
  />
  <TokenCard
    token="opacity.60"
    value="0.6"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-60)',
        }}
      />
    }
  />
  <TokenCard
    token="opacity.80"
    value="0.8"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-80)',
        }}
      />
    }
  />
  <TokenCard
    token="opacity.100"
    value="1"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-100)',
        }}
      />
    }
  />
  <TokenCard
    token="opacity.muted"
    value="0.6"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-muted)',
        }}
      />
    }
  />
  <TokenCard
    token="opacity.disabled"
    value="0.4"
    preview={
      <div
        style={{
          height: '32px',
          borderRadius: 'var(--morphink-radius-sm)',
          background: 'var(--morphink-color-foreground)',
          opacity: 'var(--morphink-opacity-disabled)',
        }}
      />
    }
  />
</TokenGrid>

## Utilities

```
mi-opacity-40
mi-opacity-60
mi-opacity-80
mi-opacity-100
```

Example: `mi-opacity-60`

## Notes for designers

TBD
