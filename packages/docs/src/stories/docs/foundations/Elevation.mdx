import { Meta, Title, Subtitle, Canvas } from '@storybook/addon-docs/blocks'
import * as PanelStories from '../../components/Panel.stories'

export const TokenGrid = ({ children }) => (
  <div
    style={{
      display: 'grid',
      gap: '12px',
      gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
    }}
  >
    {children}
  </div>
)

export const TokenCard = ({ token, value, usage, preview }) => (
  <div
    style={{
      border: '1px solid var(--morphink-color-border)',
      borderRadius: 'var(--morphink-radius-md)',
      padding: '12px',
      background: 'var(--morphink-color-card)',
    }}
  >
    <div>{preview}</div>
    <div
      style={{
        marginTop: '8px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Token
    </div>
    <div>
      <code>{token}</code>
    </div>
    <div
      style={{
        marginTop: '6px',
        fontSize: 'var(--morphink-font-size-caption)',
        lineHeight: 'var(--morphink-font-line-height-caption)',
        color: 'var(--morphink-color-muted-foreground)',
      }}
    >
      Value
    </div>
    <div>
      <code>{value}</code>
    </div>
    {usage ? (
      <div
        style={{
          marginTop: '6px',
          fontSize: 'var(--morphink-font-size-caption)',
          lineHeight: 'var(--morphink-font-line-height-caption)',
          color: 'var(--morphink-color-muted-foreground)',
        }}
      >
        {usage}
      </div>
    ) : null}
  </div>
)

<Meta title="Foundations/Elevation" />

<Title>Elevation</Title>
<Subtitle>Express information hierarchy through shadows</Subtitle>

## Overview

Elevation visually indicates depth and layering. Use shadows only on areas that need emphasis; avoid overuse.

## Token List

<TokenGrid>
  <TokenCard
    token="shadow.sm"
    value="0 1px 2px 0 rgba(0, 0, 0, 0.05)"
    usage="Subtle shadow"
    preview={
      <div
        style={{
          width: '120px',
          height: '60px',
          borderRadius: 'var(--morphink-radius-md)',
          background: 'var(--morphink-color-card)',
          boxShadow: 'var(--morphink-shadow-sm)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="shadow.md"
    value="0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)"
    usage="Standard shadow"
    preview={
      <div
        style={{
          width: '120px',
          height: '60px',
          borderRadius: 'var(--morphink-radius-md)',
          background: 'var(--morphink-color-card)',
          boxShadow: 'var(--morphink-shadow-md)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
  <TokenCard
    token="shadow.lg"
    value="0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)"
    usage="Strong shadow"
    preview={
      <div
        style={{
          width: '120px',
          height: '60px',
          borderRadius: 'var(--morphink-radius-md)',
          background: 'var(--morphink-color-card)',
          boxShadow: 'var(--morphink-shadow-lg)',
          border: '1px solid var(--morphink-color-border)',
        }}
      />
    }
  />
</TokenGrid>

## Utilities

```
mi-shadow-sm
mi-shadow-md
mi-shadow-lg
```

Example: `mi-shadow-md`

## Notes for designers

TBD
